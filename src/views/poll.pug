html
  head
    title Voting | Poll: #{poll.title}
    include includes/links.pug

  body
    h1 #{poll.title}
    ul
      each choice in poll.choices
        li
          .choice-name= choice.choice
          .choice-votes= choice.votes
          button.btn.choice-btn-vote Vote
